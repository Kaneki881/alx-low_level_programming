#include <stdio.h>

/**
 * main - Prints the alphabet.
 *  description: 'I never thought that it would pay off'
 * Return: Always 0 (Success)
 */
int i;

	for (i = 97; i < 123; i++)
	{
		if (i != 101 && i != 113)
		{
			putchar(i);
		}
	}
	putchar('\n');
	return (0);
}

